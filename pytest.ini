[pytest]
pythonpath = .
testpaths = tests
addopts = --cov --cov-report=term-missing --cov-report=xml:coverage.xml --cov-report=html:htmlcov -m "not endpoint"
markers =
    endpoint: testes que apontam para o container real via HTTP
